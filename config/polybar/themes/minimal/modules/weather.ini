[module/weather]
type = custom/script
interval = 1200
# Points to your secure hardened weather script via env var set in launch.sh
exec = ${env:DWM_SCRIPTS}/OpWeatherinfo
label = %output%
label-padding = 1
label-foreground = ${colors.fg}
